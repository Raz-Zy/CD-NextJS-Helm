apiVersion: v1
kind: Service
metadata:
    name: cd-nextjs-servive
spec:
    type: NodePort
    selector:
        app: nextjs
    ports:
    -   protocol: TCP
        port: 3001
        targetPort: 3001
        nodePort: 30002